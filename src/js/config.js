// variables that are constants, used in the whole project
// ex: the url where we request the recipes
// variáveis de config nome com letra maiuscola.
export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';
export const TIMEOUT_SEC = 10;

// dá erro no parcel, dis que não acha o config.js
// por isso coloquei no helpers.js
